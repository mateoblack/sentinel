---
phase: 93-docs-validation
plan: 03
type: execute
wave: 2
depends_on: ["93-01", "93-02"]
files_modified: []
autonomous: true
---

<objective>
Validate documentation accuracy and run infrastructure test coverage verification.

Purpose: Ensure v1.12 documentation is accurate and test coverage is complete before milestone completion.
Output: Verification report confirming documentation accuracy and test coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/QUICKSTART.md
@docs/BOOTSTRAP.md
@docs/guide/commands.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate CLI help text matches documentation</name>
  <files></files>
  <action>
Run the CLI help commands and verify they match the documented flags:

1. `sentinel init approvals --help` - verify flags match docs
2. `sentinel init breakglass --help` - verify flags match docs
3. `sentinel init sessions --help` - verify flags match docs
4. `sentinel init bootstrap --help` - verify --with-* flags documented
5. `sentinel init status --help` - verify --check-tables documented

Compare output to documentation in BOOTSTRAP.md and commands.md.
Report any discrepancies.
  </action>
  <verify>go build ./cmd/sentinel && ./sentinel init approvals --help 2>&1 | head -20</verify>
  <done>All CLI help text matches documentation</done>
</task>

<task type="auto">
  <name>Task 2: Run infrastructure package tests</name>
  <files></files>
  <action>
Run the infrastructure package tests to confirm coverage:

```bash
go test -v -cover ./infrastructure/...
```

Verify:
- All tests pass
- Coverage is adequate (target: >80%)
- No race conditions (run with -race if needed)

Also run the CLI init command tests:

```bash
go test -v ./cli/... -run "Init(Approvals|BreakGlass|Sessions)"
```
  </action>
  <verify>go test -cover ./infrastructure/... 2>&1 | tail -5</verify>
  <done>All infrastructure and CLI tests pass with adequate coverage</done>
</task>

<task type="auto">
  <name>Task 3: Verify documentation examples are valid</name>
  <files></files>
  <action>
Check that documented command examples are syntactically valid:

1. Build the CLI: `go build ./cmd/sentinel`
2. Run each documented command with --help or --plan to verify syntax:
   - `./sentinel init approvals --plan --region us-east-1 --help`
   - `./sentinel init breakglass --help`
   - `./sentinel init sessions --help`
   - `./sentinel init bootstrap --help`

Verify:
- All documented flags exist
- No typos in flag names
- Required flags are marked as required
  </action>
  <verify>go build ./cmd/sentinel && ./sentinel init --help</verify>
  <done>All documented examples use valid command syntax</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CLI help matches documentation for all init subcommands
- [ ] go test ./infrastructure/... passes
- [ ] go test ./cli/... -run Init passes
- [ ] All documented command examples are syntactically valid
- [ ] No discrepancies found between code and documentation
</verification>

<success_criteria>
- All tasks completed
- No documentation/code mismatches found
- Test suite passes
- Phase 93 documentation is accurate and complete
</success_criteria>

<output>
After completion, create `.planning/phases/93-docs-validation/93-03-SUMMARY.md`
</output>
