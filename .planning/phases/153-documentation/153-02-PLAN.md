---
phase: 153-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [README.md, docs/QUICKSTART.md]
autonomous: true
---

<objective>
Update README and Quick Start for v2.0 stable release.

Purpose: Remove alpha status, update documentation to reflect production-ready v2.0 release.
Output: README.md and QUICKSTART.md accurately describe v2.0 features and setup process.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@README.md
@docs/QUICKSTART.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md for v2.0 stable release</name>
  <files>README.md</files>
  <action>
Update README.md with the following changes:

1. Remove the "Alpha Release" banner:
   - DELETE the line: `> **Alpha Release** - APIs and features may change. Feedback welcome via issues.`

2. Add a stable release badge or note:
   - Add after the badge: `> **v2.0 Stable** - Production-ready with comprehensive security hardening.`

3. Verify Quick Start section matches v2.0 setup:
   - Installation command uses @latest (correct)
   - Bootstrap command matches current CLI flags
   - Example exec command matches current syntax

4. Verify Features tables are accurate:
   - Check that all v1.14-v2.0 features are listed
   - Policy signing should be listed under Access Control
   - Intune MDM support should be mentioned under device posture

5. Update Documentation section if needed:
   - Ensure SCP_REFERENCE.md is listed (will be created by 153-01)
   - Ensure all docs links work

Do NOT change the example policy YAML or the architectural diagram - they are correct.
  </action>
  <verify>grep -c "Alpha Release\|v2.0 Stable" README.md</verify>
  <done>README.md shows v2.0 Stable, not Alpha Release</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update QUICKSTART.md for v2.0</name>
  <files>docs/QUICKSTART.md</files>
  <action>
Review and update QUICKSTART.md:

1. Check Prerequisites section:
   - Verify Go version requirement (should be Go 1.25+, which is correct)
   - Verify AWS credentials guidance is accurate

2. Check Quick Setup steps:
   - Step 1: config generate command - verify flags match CLI
   - Step 2: init bootstrap command - verify works with current CLI
   - Step 3: exec command - verify syntax is correct

3. Check DynamoDB table setup commands:
   - Verify init approvals/breakglass/sessions commands match CLI
   - Verify unified bootstrap --with-* flags are documented

4. Add v2.0-specific guidance if missing:
   - Policy signing setup (reference POLICY_SIGNING.md)
   - KMS key requirements for signed policies
   - Device posture setup (reference DEVICE_POSTURE.md)

5. Verify all cross-references to other docs are valid:
   - PERMISSIONS.md link
   - POLICY_SIGNING.md link
   - DEVICE_POSTURE.md link

Do NOT add time estimates. Focus on accuracy of commands and cross-references.
  </action>
  <verify>grep -c "sentinel init\|sentinel exec\|sentinel config" docs/QUICKSTART.md</verify>
  <done>QUICKSTART.md accurately reflects v2.0 setup process with working commands</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] README.md no longer shows "Alpha Release"
- [ ] README.md shows v2.0 Stable status
- [ ] README.md feature tables include v2.0 features
- [ ] QUICKSTART.md commands match current CLI
- [ ] All documentation cross-references are valid
</verification>

<success_criteria>

- All tasks completed
- README reflects production-ready v2.0 status
- Quick start guide is accurate and complete
- No broken documentation links
</success_criteria>

<output>
After completion, create `.planning/phases/153-documentation/153-02-SUMMARY.md`
</output>
